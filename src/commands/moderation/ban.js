const { ApplicationCommandOptionType} = require('discord.js');

module.exports = {
  name: 'ban',
  description: 'Bans a member from the server',
  // devOnly: Boolean
  // testOnly: Boolean
  options: Object[''],
}